node_modules/
dist/
coverage/
.DS_Store
*.log

.env
.env.*
!.env.example

# generated Prisma client — run `pnpm prisma:generate`
src/generated/

# machine-local, regenerated by fpl-orchestrator/scripts/link-skills.sh
.claude/skills
.claude/settings.json
.claude/settings.local.json

# machine-local cache of the third-party per-gameweek archive (B-007 Phase 2b).
# ~5 MB per season, fetched on demand; the source repo is ~182 MB under an unclear licence and
# is deliberately not vendored here.
.archive-cache/
reports/datasets/*.csv
tools/fit-v4/venv/
